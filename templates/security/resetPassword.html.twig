{% extends 'base.html.twig' %}

{% block title %}{{ 'Réinitialisation du mot de passe'|trans }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/reset_password.css') }}">
{% endblock %}

{% block body %}
<div class="reset-container">
    <div class="reset-card">
        <h1 class="reset-title">{{ 'Réinitialisez votre mot de passe'|trans }}</h1>

        {{ form_start(resetForm, { 'attr': { 'class': 'reset-form' } }) }}
            <div class="form-group">
                {{ form_label(resetForm.password, null, { 'label_attr': { 'class': 'form-label', 'aria-label': 'New password' } }) }}
                {{ form_widget(resetForm.password, { 'attr': { 'class': 'form-input', 'aria-required': 'true', 'aria-describedby': 'passwordHelp' } }) }}
                {{ form_errors(resetForm.password) }}
                <small id="passwordHelp" class="form-help">{{ 'Votre mot de passe doit comporter au moins 8 caractères.'|trans }}</small>
            </div>

            <button type="submit" class="btn-primary" aria-label="Submit to reset your password">
                {{ 'Réinitialiser le mot de passe'|trans }}
            </button>
        {{ form_end(resetForm) }}
    </div>
</div>
{% endblock %}
